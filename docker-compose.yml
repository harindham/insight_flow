services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: insightflow-backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    restart: unless-stopped
